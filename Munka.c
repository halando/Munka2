#include <stdio.h>

int main() {
    int a, b;

    printf("Add meg az első számot: ");
    scanf("%d", &a);

    printf("Add meg a második számot: ");
    scanf("%d", &b);

    printf("Az összeg: %d\n", a + b);

    return 0;
}